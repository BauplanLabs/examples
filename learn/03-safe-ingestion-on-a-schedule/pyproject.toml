[project]
name = "03-safe-ingestion-on-a-schedule"
version = "0.1.0"
description = "Write-Audit-Publish pattern for safe lakehouse ingestion using Prefect and Bauplan"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bauplan>=0.1.9",
    "prefect>=3.6.20",
]
